---
    name: Modal
---

import { PropsTable, Playground } from 'docz'

import Modal from '../../src/components/molecules/Modal';
import Button from '../../src/components/atoms/Button';
import ModalButtonExample from './ModalButtonExample';

import '../../src/styles/_modals.scss';
import '../../src/styles/_button.scss';
import '../../src/styles/_backdrop.scss';
import './modalDocs.scss';


# Modal
## Installation
Using the react components:

`import Modal from @vividseats/vivid-design-patterns/dist/molecules/Modal;`

Using the scss files:

`@import @vividseats/vivid-design-patterns/src/styles/_modals.scss;`

## Basic example
`
React: className="vp-modal" dataState="opened"
HTML: class="vp-modal" data-state="opened"
`
<Playground>
    <Modal dataState="opened">
        Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
        Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure
        dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non
        proident, sunt in culpa qui officia deserunt mollit anim id est laborum.
    </Modal>
</Playground>

## Props
<PropsTable of={Modal}/>

## Props example
`dataState="opened" title="Modal Title" type="sheet" onClose={() => window.alert('on close fired')`
<Playground>
    <Modal dataState="opened" title="Modal Title" type="sheet" onClose={() => window.alert('on close fired')}>
        This sheet modal is uses a prop for the modal title, passes in the body text, and uses the default modal footer and dismiss button.
    </Modal>
</Playground>

## Sub component example
```
<Modal>
    <Modal.Header />
    <Modal.Body />
    <Modal.Footer />
</Modal>
```
<Playground>
    <Modal dataState="opened">
        <Modal.Header className="vp-type-headline4">Custom Modal H4 Header</Modal.Header>
        <Modal.Body>
            <p>This modal is using subcomponents for the modal header, body, and footer.</p>
        </Modal.Body>
        <Modal.Footer>
            <Button importance="tertiary">Custom button</Button>
        </Modal.Footer>
    </Modal>
</Playground>

## Example with external button
### Modal is rendered by a component that passes in a changing dataState prop
`dataState={!!state.opened ? 'opened' : 'closed'}`
<Playground>
    <ModalButtonExample />
</Playground>
